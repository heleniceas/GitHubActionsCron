name: Hourly Hello World CRON

on:
  workflow_dispatch:
  schedule:
    - cron: "48 * * * *"
    - cron: "10 * * * *"


jobs:
  say_hello:
    runs-on: ubuntu-latest
    steps:
    - name: Say Hello
      run: |
        echo "Hello World! It's $(date)"
